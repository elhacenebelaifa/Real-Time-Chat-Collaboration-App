FROM node:14-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install

# Copy source (overridden by volume mount at runtime)
COPY . .

EXPOSE 3000

CMD ["npm", "run", "dev"]
